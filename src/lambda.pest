lam = _{ "λ" | "\\" }

var = { ALPHABETIC ~ NUMBER* }
abs_bound = { ALPHABETIC ~ NUMBER* }
abs = { lam ~ abs_bound ~ "." ~ expr }
app = { "(" ~ expr ~ ")" }
term = { abs | app | var }
expr = { term ~ (" " ~ term)* }

lambda = { SOI ~ expr ~ EOI }